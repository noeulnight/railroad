{
  "name": "railroad",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@10.27.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "dev:frontend": "turbo dev --filter=@railroad/frontend",
    "dev:backend": "pnpm --filter @railroad/backend start:dev",
    "build:frontend": "turbo build --filter=@railroad/frontend",
    "build:backend": "pnpm --filter @railroad/backend build",
    "lint:frontend": "turbo lint --filter=@railroad/frontend",
    "lint:backend": "pnpm --filter @railroad/backend lint",
    "test:frontend": "turbo test --filter=@railroad/frontend",
    "test:backend": "pnpm --filter @railroad/backend test"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
